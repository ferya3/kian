<?php

use App\Models\SiteMedia;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\Schema;

/**
 * جایگاه‌های تازه‌ی رسانه را به نصبِ موجود می‌رساند.
 *
 * ردیف‌های جدول site_media فقط یک‌بار ساخته می‌شوند — داخلِ همان مهاجرتی که
 * جدول را می‌سازد. پس افزودنِ کلید تازه به SiteMedia::SLOTS برای نصبِ تازه
 * کافی است ولی برای سایتی که بالا آمده نه: کلید در کد هست، ردیفش نیست، و
 * جایگاه در پنل دیده نمی‌شود — یعنی تصویری که هیچ‌کس نمی‌تواند آپلودش کند.
 *
 * sync فقط کمبودها را اضافه می‌کند و به ردیف‌های ناشناس دست نمی‌زند، پس
 * اجرای دوباره‌اش بی‌خطر است.
 *
 * شش جایگاهِ «چرا سفال؟» همین حالا از این راه می‌آیند. برای هر دسته‌ی تازه‌ی
 * بعدی هم یک مهاجرتِ مشابه لازم است.
 */
return new class extends Migration
{
    public function up(): void
    {
        if (! Schema::hasTable('site_media')) {
            return;
        }

        SiteMedia::sync();
    }

    public function down(): void
    {
        /*
         * عمداً خالی.
         *
         * برگرداندن یعنی حذفِ ردیف‌هایی که ممکن است تصویر آپلودشده داشته
         * باشند؛ جایگاهِ بی‌استفاده هیچ ضرری ندارد ولی تصویرِ ازدست‌رفته دارد.
         */
    }
};
