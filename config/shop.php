<?php

/*
|--------------------------------------------------------------------------
| فروشگاه چندفروشنده‌ای
|--------------------------------------------------------------------------
|
| کل فروشگاه پشت یک کلید است و پیش‌فرضش خاموش. وقتی خاموش باشد:
|
|   — هیچ مسیری ثبت نمی‌شود (نه ویترین، نه سبد، نه تسویه)؛
|   — هیچ منبعی در پنل مدیریت دیده نمی‌شود؛
|   — هیچ پیوندی در منو و فوتر نیست.
|
| جدول‌ها اما ساخته می‌شوند. جدولِ خالیِ بی‌استفاده هزینه‌ای ندارد، ولی
| مهاجرتی که روز فعال‌سازی باید روی دیتابیسِ پر اجرا شود، دارد.
|
| روشن‌کردنش یک متغیر در .env است و بعد پاک‌کردن کش پیکربندی:
|
|   SHOP_ENABLED=true
|   php artisan config:clear
|
*/

return [

    'enabled' => (bool) env('SHOP_ENABLED', false),

    /*
    | واحد پول.
    |
    | مبلغ‌ها همه عدد صحیح‌اند و به «تومان» ذخیره می‌شوند — نه اعشاری. پول
    | با float جمع زده نمی‌شود؛ یک ریال خطای گردکردن در فاکتور، بی‌اعتمادی
    | می‌سازد. اگر روزی واحد عوض شد، همین‌جا عوض می‌شود و نه در ویوها.
    */
    'currency' => env('SHOP_CURRENCY', 'تومان'),

    /*
    | سقف تعداد در هر ردیف سبد.
    |
    | فقط یک مهار است تا عددِ نامعقول وارد نشود؛ موجودیِ واقعیِ هر عرضه،
    | جداگانه و در خودِ عرضه بررسی می‌شود.
    */
    'max_quantity' => 9999,

    /*
    | وضعیت‌های سفارش.
    |
    | در نبودِ درگاه پرداخت، چرخه‌ی سفارش با تماس فروشنده جلو می‌رود؛ پس
    | وضعیت‌ها همان مراحل واقعیِ این گفت‌وگو هستند و نه مراحل پرداخت.
    */
    'statuses' => [
        'new' => 'ثبت‌شده',
        'confirmed' => 'تأییدشده',
        'shipped' => 'ارسال‌شده',
        'done' => 'تحویل‌شده',
        'canceled' => 'لغوشده',
    ],

];
